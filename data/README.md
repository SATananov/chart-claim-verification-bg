# Data folders

- `raw` contains the original downloaded data
- `processed` contains cleaned tables
- `generated/charts` contains chart images created by the project

Large generated files should not be committed until their size and license are checked.
